{
  "name": "solid-css",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "watch:sass": "sass scss/main.scss dist/style.css -w",
    "build": "sass scss/main.scss dist/style.css && postcss dist/style.css --output dist/style.css"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss-cli": "^11.0.0",
    "sass": "^1.80.7"
  },
  "browserslist": "> 1%, last 2 versions, not dead"
}
